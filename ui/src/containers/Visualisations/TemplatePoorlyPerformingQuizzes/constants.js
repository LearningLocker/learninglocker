import { LEADERBOARD_IMAGE } from 'ui/components/VisualiseIcon/assets';

export const title = 'Which quizzes are performing poorly?';
export const description = 'Which quizzes are performing poorly?';
export const image = LEADERBOARD_IMAGE;
