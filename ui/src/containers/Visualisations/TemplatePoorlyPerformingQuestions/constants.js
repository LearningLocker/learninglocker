import { LEADERBOARD_IMAGE } from 'ui/components/VisualiseIcon/assets';

export const title = 'Which questions are performing poorly?';
export const description = 'Which questions are performing poorly?';
export const image = LEADERBOARD_IMAGE;
