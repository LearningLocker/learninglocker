// @ts-check

/**
 * @param {number} firstCount
 * @param {number} secondCount
 */
const getTupleDataKey = (firstCount, secondCount) => {
  return `${firstCount}-${secondCount}`;
};

export default getTupleDataKey;
